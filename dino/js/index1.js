document.addEventListener("keydown", (event) => {
  // event.key 또는 event.code가 'ArrowUp'인지 확인
  if (event.key === "ArrowUp") {
    console.log("화살표 위 키가 눌렸습니다!");
    // 여기서 원하는 동작을 실행합니다 (예: 스크롤 이동, 게임 캐릭터 조작 등)
  }
});
